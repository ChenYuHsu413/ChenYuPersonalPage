# 2026-06-01 工作日誌

## 今日工作總覽

今天主要完成個人作品集網站的功能補強、檔案結構整理、文件重寫與 GitHub 推送準備。工作內容包含頁首即時時間、GitHub Repo 連結、README 更新、專案檔案清理、圖片資源整理、CSS / JavaScript 拆檔，以及 README 預覽截圖。

## 完成事項

### 1. 頁首主視覺新增即時時間

- 在首頁 hero 區塊加入目前時間與日期顯示。
- 使用 JavaScript 每秒更新時間。
- 時間區塊維持原本作品集視覺風格。

相關檔案：
- `index.html`
- `style.css`
- `script.js`

### 2. 頁首加入 GitHub Repo 連結

- 在導覽列新增 GitHub 圖示與連結。
- 連結目標為 `https://github.com/ChenYuHsu413/ChenYu`。
- README 開頭也補上 Repo 連結。

相關檔案：
- `index.html`
- `README.md`

### 3. 推送前整合遠端變更

- 建立 commit 後推送到 GitHub。
- 遠端 `main` 曾有本機尚未同步的更新，因此先執行 rebase。
- rebase 無衝突，後續成功推送到 `origin/main`。

相關指令：
```bash
git pull --rebase origin main
git push origin main
```

### 4. 清理不必要設定檔

- 檢查 `.vscode/extensions.json`，確認只包含 VS Code 擴充套件推薦，非網站必要檔案。
- 依需求刪除 `.vscode/`。
- 檢查 `.gitignore` 用途後，依需求刪除。

相關變更：
- 刪除 `.vscode/extensions.json`
- 刪除 `.gitignore`

### 5. 整理圖片資源

- 建立 `sources/` 資料夾。
- 將 `workflow_infographic.jpg` 移入 `sources/`。
- 更新 `工作報告.md` 的圖片路徑。

相關檔案：
- `sources/workflow_infographic.jpg`
- `工作報告.md`

### 6. 合併舊對話紀錄到 dev-log

- 依照 `AGENT.md` 規則整理開發紀錄。
- 將 `log.md` 的有效內容整理後接到 `docs/dev-log.md`。
- 合併完成後刪除 `log.md`。
- 將 `工作報告.md` 的核心檔案清單從 `log.md` 改為 `docs/dev-log.md`。

相關檔案：
- `docs/dev-log.md`
- `工作報告.md`

### 7. 拆分 HTML / CSS / JavaScript

- 將 `index.html` 中的內嵌 CSS 抽出為 `style.css`。
- 將 `index.html` 中的內嵌 JavaScript 抽出為 `script.js`。
- `index.html` 現在只保留頁面結構與內容。
- README 重新整理成符合目前檔案結構的版本。

相關檔案：
- `index.html`
- `style.css`
- `script.js`
- `README.md`

### 8. 產生首頁預覽截圖並加入 README

- 使用本機 Chrome headless 擷取首頁首屏。
- 截圖儲存為 `sources/demo-screenshot.png`。
- 在 README 的 Live Demo 連結下方加入預覽圖片。
- 同時將 README 的檔案樹改成 ASCII 格式，避免特殊符號顯示亂碼。

相關檔案：
- `sources/demo-screenshot.png`
- `README.md`

## 目前專案檔案結構

```text
ChenYu/
|-- index.html
|-- style.css
|-- script.js
|-- README.md
|-- AGENT.md
|-- 工作報告.md
|-- docs/
|   |-- dev-log.md
|   `-- 2026-06-01-work-log.md
`-- sources/
    |-- demo-screenshot.png
    `-- workflow_infographic.jpg
```

## 驗證結果

- 已確認 `index.html` 改為引用 `style.css` 與 `script.js`。
- 已確認 `script.js` 保留即時時間更新邏輯。
- 已確認 `sources/demo-screenshot.png` 成功產生且不是空白畫面。
- 已確認 README 的 Live Demo 下方已加入預覽圖片。
- 已確認 `log.md` 已刪除，開發紀錄集中在 `docs/dev-log.md`。

## 後續建議

- 推送後到 GitHub Repo 檢查 README 圖片是否正常顯示。
- 到 GitHub Pages 檢查拆檔後的 CSS 與 JavaScript 是否載入正常。
- 若未來繼續更新網站，維持 `index.html`、`style.css`、`script.js` 分離的結構。

